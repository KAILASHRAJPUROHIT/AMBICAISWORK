FORCE CHANGE PASSWORD RUNTIME TRACE

Scope:
- Diagnosis only.
- No source code changes made.

Runtime confirmation:
- Backend is current.
- OpenAPI contains /api/auth/change-password.

Database state:
- ACC-01: password_reset_required=1, is_active=1
- BILLING: password_reset_required=1, is_active=1
- DEV-01: password_reset_required=1, is_active=1
- OWNER-01: password_reset_required=0, is_active=1

Active reset-required session inspected:
- employee_id=BILLING
- session_token=XS7pU1hxfDcwzKf6AMtT1bZGa6RFrPjS

1. /api/auth/verify RESPONSE BODY

Successful OTP response:
- Not available to inspect retroactively from the already-completed login.
- I did not insert OTP records or mutate auth state just to manufacture a successful verify response during a diagnosis-only pass.

Runtime probe with invalid OTP:
- Request:
  POST http://127.0.0.1:8000/api/auth/verify
  Body: {"employee_id":"BILLING","otp_code":"000000"}
- Response:
  STATUS=401
  BODY={
    "detail": "Invalid or expired OTP"
  }

Current backend source for successful /api/auth/verify returns:
- top-level password_reset_required
- user.password_reset_required
- user.reset_required

Relevant response shape in backend/review_api.py:
- "password_reset_required": bool(user.password_reset_required)
- "user": {
    "password_reset_required": bool(user.password_reset_required),
    "reset_required": bool(user.password_reset_required)
  }

Conclusion:
- Actual successful verify response body was not captured in Network during this pass.
- Source/runtime route availability indicate successful verify should include the flag.
- If a fresh successful Network trace does not include password_reset_required or user.reset_required, the root cause is /api/auth/verify response missing the flag at runtime.

2. /api/auth/me RESPONSE BODY

Request:
- GET http://127.0.0.1:8000/api/auth/me
- Header:
  X-Session-Token: XS7pU1hxfDcwzKf6AMtT1bZGa6RFrPjS

Response:
{
  "name": "HITESH KUMAR",
  "role": "STAFF",
  "employee_id": "BILLING",
  "password_reset_required": true,
  "reset_required": true
}

Conclusion:
- password_reset_required is present in /api/auth/me.
- /api/auth/me is not currently overwriting the reset-required state to false for this reset-required user.

3. PROTECTED API ENFORCEMENT CHECK

Request:
- GET http://127.0.0.1:8000/api/dashboard/live
- Header:
  X-Session-Token: XS7pU1hxfDcwzKf6AMtT1bZGa6RFrPjS

Response:
- STATUS=403
- BODY={
    "detail": "Password change required before accessing this resource.",
    "password_reset_required": true
  }

Conclusion:
- Backend protected API enforcement is active at runtime.
- Dashboard data API is blocked until password change for reset-required sessions.

4. LoginPage.tsx VERIFY HANDLER

File:
- frontend/src/pages/LoginPage.tsx

Behavior:
- verifyOTP response is read into data.
- token is stored in:
  - localStorage["aradhana_session_token"]
  - localStorage["session_token"]
- resetRequired is calculated from:
  - data.password_reset_required
  - data.user?.password_reset_required
  - data.user?.reset_required
- localStorage["user"] is rewritten with:
  - password_reset_required: resetRequired
  - reset_required: resetRequired
- If resetRequired is true:
  - localStorage["password_reset_required"]="true"
  - password and OTP component state are cleared
  - window.location.href="/change-password"

Conclusion:
- Current LoginPage source does not ignore the flag.
- It accepts either the new top-level flag or the legacy nested reset_required flag.

5. App.tsx USER STATE HYDRATION

File:
- frontend/src/App.tsx

ProtectedRoute behavior:
- Reads token via getSessionToken().
- Reads localStorage["user"].
- resetRequired is true if any of these are true:
  - user.password_reset_required
  - user.reset_required
  - localStorage["password_reset_required"] === "true"
- If resetRequired=true and path is not /change-password:
  - redirects to /change-password.
- If resetRequired=false and path is /change-password:
  - redirects to Dashboard.

Hydration behavior:
- On app startup, getMe() is called.
- If /api/auth/me returns password_reset_required or reset_required:
  - localStorage["password_reset_required"]="true"
- Otherwise:
  - localStorage["password_reset_required"] is removed.

Runtime /api/auth/me currently returns password_reset_required=true for BILLING, so hydration preserves the reset-required state.

Conclusion:
- Current App.tsx does not ignore the flag.
- Current /api/auth/me does not overwrite it to false for BILLING.

6. SERVED FRONTEND BUNDLE CHECK

Served build artifact:
- frontend/dist/assets/index-B6j8e1H8.js

Bundle contains:
- password_reset_required checks
- reset_required checks
- /change-password route
- /api/auth/change-password call
- Login verify handler redirecting to /change-password when resetRequired=true
- ProtectedRoute redirecting resetRequired sessions to /change-password

Conclusion:
- The built frontend currently on disk contains the forced-change logic.

7. FINAL DETERMINATION

Is password_reset_required absent?
- /api/auth/me: NO. It is present and true for BILLING.
- protected API 403 body: NO. It is present and true.
- /api/auth/verify successful response: NOT CAPTURED in this diagnosis pass. Source indicates it should be present.

Is password_reset_required present but ignored?
- Current LoginPage.tsx: NO. It reads top-level and nested flags.
- Current App.tsx ProtectedRoute: NO. It checks user.password_reset_required, user.reset_required, and localStorage["password_reset_required"].

Is password_reset_required overwritten by /api/auth/me?
- Current runtime for BILLING: NO. /api/auth/me returns password_reset_required=true and reset_required=true.

Most likely root cause of the observed "not redirected" login:
- The browser was using an already-loaded stale SPA bundle from before the forced-change-password frontend logic was loaded.

Why:
- Current backend is enforcing reset-required sessions.
- Current /api/auth/me returns the reset flag.
- Current protected API calls are blocked.
- Current served dist bundle contains the redirect logic.
- If the user logged in from a tab that had loaded the old LoginPage JavaScript before the rebuild/restart, that old verify handler would store the token/user and navigate to Dashboard without checking password_reset_required.

Remaining proof needed:
- Hard refresh /login or open a fresh browser tab.
- Login with a reset-required temporary-password user.
- Capture successful POST /api/auth/verify Network response.
- Expected response must include one of:
  - password_reset_required=true
  - user.password_reset_required=true
  - user.reset_required=true
- Expected browser result:
  - redirect to /change-password
  - Dashboard API calls return 403 until password is changed.
