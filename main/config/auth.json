{
  "login_required": false,
  "_comment": "Set login_required true to restore the password + email 2FA gate. Disabled 2026-09-03 at the owner's request: single-operator tool on the shop LAN. The /login and /verify-otp routes are untouched, so flipping this back on restores the original behaviour with no code change."
}