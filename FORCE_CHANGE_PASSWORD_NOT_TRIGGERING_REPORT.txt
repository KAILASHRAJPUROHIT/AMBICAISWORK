FORCE CHANGE PASSWORD NOT TRIGGERING REPORT

Scope:
- Diagnosis only.
- No application code changed.

1. DB CHECK

Database:
- C:\Users\kaila\aradhana-payment-auditor\aradhana-payment-auditor\aradhana_dev.db

Active users currently requiring password reset:
- employee_id=ACC-01
  password_reset_required=1
  is_active=1
- employee_id=BILLING
  password_reset_required=1
  is_active=1
- employee_id=DEV-01
  password_reset_required=1
  is_active=1

Current visible browser user:
- OWNER-01 / Aradhana Owner
- password_reset_required=0
- is_active=1

Conclusion:
- DB flag is set for reset-required operational users.
- Root cause is not that the DB flag is universally missing.

2. /api/auth/verify RESPONSE

Browser Network:
- A fresh OTP verification request was not available to inspect retroactively in the current browser session.

Source code in backend/review_api.py:
- /api/auth/verify currently returns:
  - top-level password_reset_required
  - user.password_reset_required
  - user.reset_required

Live running backend check:
- /openapi.json from http://127.0.0.1:8000 does not include /api/auth/change-password.
- This proves the running backend process is not serving the latest backend file currently on disk.

Likely live /api/auth/verify status:
- The running backend is stale.
- It may still return only the older user.reset_required shape, or no top-level password_reset_required.
- LoginPage can read user.reset_required, so /verify alone is probably not the only failure.

3. LoginPage.tsx INSPECTION

File:
- frontend/src/pages/LoginPage.tsx

Current source behavior:
- Reads reset flag from:
  - data.password_reset_required
  - data.user.password_reset_required
  - data.user.reset_required
- Stores:
  - localStorage["user"].password_reset_required
  - localStorage["user"].reset_required
  - localStorage["password_reset_required"]="true"
- Navigates to:
  - /change-password when resetRequired=true

Conclusion:
- Current LoginPage source does read the verify response and routes to /change-password.

4. App.tsx / ProtectedRoute INSPECTION

File:
- frontend/src/App.tsx

Current source behavior:
- ProtectedRoute reads:
  - user.password_reset_required
  - user.reset_required
  - localStorage["password_reset_required"] === "true"
- If true, blocks protected app pages and redirects to /change-password.
- If false and current path is /change-password, redirects to Dashboard.

Conclusion:
- Current frontend source is designed to block Dashboard when reset flag remains true.

5. /api/auth/me / SESSION REFRESH CHECK

Live request:
- GET http://127.0.0.1:8000/api/auth/me
- Header: X-Session-Token for active BILLING session

Live response:
- {"name":"HITESH KUMAR","role":"STAFF","employee_id":"BILLING"}

Expected response:
- Should include password_reset_required=true for BILLING.

Live protected route check:
- GET http://127.0.0.1:8000/api/dashboard/live
- Header: same BILLING reset-required session
- Status: 200
- Expected: 403 with password_reset_required=true until password is changed.

Conclusion:
- Running backend is not enforcing reset-required sessions.
- Running /api/auth/me is missing password_reset_required.
- Running protected API middleware is not blocking reset-required sessions.

6. ROOT CAUSE

Primary root cause:
- Stale running backend process.

Evidence:
- Backend source on disk contains:
  - /api/auth/change-password
  - /api/auth/me password_reset_required
  - protected API reset-required block
- Live http://127.0.0.1:8000/openapi.json does not include /api/auth/change-password.
- Live /api/auth/me omits password_reset_required for BILLING even though DB has BILLING.password_reset_required=1.
- Live /api/dashboard/live allows BILLING session with password_reset_required=1.

Frontend source status:
- Current frontend source and built dist contain reset-required routing logic.

Failure chain:
1. User logs in with temporary password.
2. OTP verify may produce a reset flag.
3. Frontend stores reset flag and attempts to route /change-password.
4. App startup calls /api/auth/me.
5. Running stale backend returns user without password_reset_required.
6. Frontend overwrites local user state with missing reset flag and removes localStorage["password_reset_required"].
7. ProtectedRoute sees resetRequired=false and redirects /change-password to Dashboard.

Classification:
- DB flag not set: NO
- verify response missing flag: POSSIBLE on live stale backend, not confirmed from Network
- frontend ignores flag: NO in current source
- session/me response missing flag: YES on live running backend
- route guard not enforcing flag: NO in current source, but live stale backend allows protected APIs
- proxy issue: NO evidence

7. MINIMAL NEXT ACTION

- Restart the backend process serving http://127.0.0.1:8000 so it loads current backend/review_api.py.
- Recheck:
  - GET /openapi.json includes /api/auth/change-password
  - GET /api/auth/me for BILLING includes password_reset_required=true
  - GET /api/dashboard/live for BILLING reset-required session returns 403 password_reset_required=true
- Then repeat browser login with the temporary password and inspect /api/auth/verify Network response.
