# AIS Local Control Plane — local legacy runtime

Canonical source copy moved to `C:\AradhanaSystems\platform\ais-local-control-plane` on 2026-09-07. It is a dependency-free Node 24 application; no package lock or `npm install` is required.

The currently running fallback remains under `C:\DEEPSEEK HARNESS\control-plane` until dashboard/watchdog cutover is separately verified.
