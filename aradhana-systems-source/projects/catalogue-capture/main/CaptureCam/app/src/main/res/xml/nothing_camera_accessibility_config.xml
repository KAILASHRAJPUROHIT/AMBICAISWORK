<?xml version="1.0" encoding="utf-8"?>
<!-- Scoped to Nothing Camera only: this service must never see or act on
     any other app's UI. windowStateChanged/windowContentChanged is enough
     to know when CameraActivity's shutter node exists; no need for the
     broader "all events" flag. -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRetrieveInteractiveWindows"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100"
    android:packageNames="com.nothing.camera" />
